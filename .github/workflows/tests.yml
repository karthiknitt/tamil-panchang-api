name: Tests

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.11"]

    steps:
      - uses: actions/checkout@v7

      - name: Install uv
        uses: astral-sh/setup-uv@v10
        with:
          version: "0.10.4"
          enable-cache: true
          prune-cache: false
          python-version: ${{ matrix.python-version }}

      - name: Install dependencies
        run: uv sync --frozen

      - name: Lint with ruff
        run: |
          uv run ruff check .
          uv run ruff format --check .

      - name: Run muhurta rule unit tests
        run: uv run python test_muhurta.py

      - name: Start API server
        run: |
          uv run uvicorn app:app --host 0.0.0.0 --port 8000 &
          sleep 5

      - name: Run tests
        run: uv run python test_api.py
